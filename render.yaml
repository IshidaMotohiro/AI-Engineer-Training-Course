services:
  - type: web
    name: tse-stock-search
    env: python
    runtime: python-3.12
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
